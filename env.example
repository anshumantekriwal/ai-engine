# AI Engine Environment Variables

# Required — Anthropic API key (agent code generation requires Anthropic)
ANTHROPIC_API_KEY=sk-ant-...

# Optional — OpenAI key (only needed if backtest spec generator uses OpenAI)
# OPENAI_API_KEY=sk-...

# Provider (must be "anthropic" for code generation)
AI_PROVIDER=anthropic

# Model for backtest spec generation (defaults to claude-sonnet-4-5)
AI_MODEL=

# Model override for agent code generation (defaults to AI_MODEL)
AGENT_MODEL=

# Max agentic loop turns before timeout (default: 15)
AGENT_MAX_TURNS=15

# Enable post-generation lint/syntax checks + self-correction (default: true)
VALIDATION_ENABLED=true

# Server
HOST=0.0.0.0
PORT=8000
